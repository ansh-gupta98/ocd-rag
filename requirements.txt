fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.0.1
pydantic==2.8.2

# HTTP only — no torch, no transformers, no sentence-transformers
requests==2.32.3
huggingface-hub==0.24.7

# LangChain core (no ML deps)
langchain==0.3.0
langchain-community==0.3.0
langchain-text-splitters==0.3.0

# Vector store — CPU only, no torch
faiss-cpu==1.8.0

# Document loaders
pypdf==4.3.1
numpy==1.26.4